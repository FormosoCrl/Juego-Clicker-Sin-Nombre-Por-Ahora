extends Node

static func execute(
		skill_id: String,
		caster: Combatant,
		targets: Array,
		all_combatants: Array) -> void:
	push_warning("SkillsGeneric: habilidad no implementada -> " + skill_id)
